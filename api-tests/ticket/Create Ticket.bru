meta {
  name: Create Ticket
  type: http
  seq: 1
}

post {
  url: {{BASE_URL}}/users/:userId/tickets
  body: json
  auth: inherit
}

params:path {
  userId: 1
}

body:json {
  {
    "description": "This is a ticket"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
