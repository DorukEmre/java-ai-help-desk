meta {
  name: Get all Tickets by userId
  type: http
  seq: 2
}

get {
  url: {{BASE_URL}}/users/:userId/tickets
  body: none
  auth: inherit
}

params:path {
  userId: 1
}

settings {
  encodeUrl: true
  timeout: 0
}
