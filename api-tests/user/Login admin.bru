meta {
  name: Login admin
  type: http
  seq: 4
}

post {
  url: {{BASE_URL}}/login
  body: json
  auth: inherit
}

body:json {
  {
    "username": "admin",
    "password": "Admin1"
  }
  
}

settings {
  encodeUrl: true
  timeout: 0
}
